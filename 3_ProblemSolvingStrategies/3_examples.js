/**
 * Step 2: Explore Examples
 * 
 * User Stories
 * Unit Tests
 * 
 * 1. Start with simple examples.
 *    - Include input and output.
 * 2. Progress to more complex examples.
 * 3. Edge cases
 *    - Empty inputs
 *    - Invalid inputs
 */

/**
 * Write a function which takes in a string and
 * returns counts of each character in a string.
 */

// charCount("aaaa"); // {a:4}
// charCount("hello"); // {h:1, e:1, l:2, o:1}

// Do we also want to return characters that aren't there?
// "my phone number is 1111111111" - Do we return spaces, $, _ ?
// "Hello hi" - Do we ignore case?

// charCount(""); - What do we return?
